describe("Conjunto de testes que serão realizados", () => {
    test("deve <retorno_esperado> se/quando <condição>", () => {
    // test code
    });
    test("deve <retorno_esperado> se/quando <condição>", async() => {
    // test code
    });
    // .
});